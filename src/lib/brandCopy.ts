/**
 * Homepage / chrome marketing strings — single source so Hero, Footer, and SEO
 * cannot drift. Positioning lives in `.agents/product-marketing.md`.
 */

/** Category noun for the first five seconds. */
export const BRAND_CATEGORY = "Open-source infrastructure for molecular simulation";

/** Subhead under the wordmark — modular install story, no slang. */
export const BRAND_SUBHEAD =
  "Independently installable packages — from system build to traceable run.";

/** Short chrome/footer line (same claim as the subhead, compressed). */
export const BRAND_TAGLINE =
  "Open-source packages for molecular simulation — install one, or take the stack.";

/** Schema / meta description — factual, agent-aware once. */
export const BRAND_DESCRIPTION =
  "MolCrafts is open-source infrastructure for molecular simulation workflows. Packages install independently and share one record format so results stay reusable across people, tools, and agents.";
