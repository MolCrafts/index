# Open questions

- Which test runner and layout should become the first real automated test
  harness? Until decided, lint plus type checking remains the project gate and
  `build.test_single` / `build.coverage` stay intentionally unset.
